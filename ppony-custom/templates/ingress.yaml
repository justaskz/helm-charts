---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.name }}
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  rules:
  - host: ppony.infra.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ppony-hello-world
            port:
              number: 80
  tls:
    - secretName: infra-certs
